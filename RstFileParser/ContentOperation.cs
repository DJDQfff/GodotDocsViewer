using System;
using System.Collections.Generic;
using System.Diagnostics;
using System.IO;
using System.Text;
using System.Linq;
using MyStandard20Library;

namespace RstFileParser
{
    public static class ContentOperation
    {
        //public static InLineItem SearchInLineItem (this string str)
        //{
        //    string[] pairs = { "*", "**", "``" };
        //    var dictionary = str.IndexOfAny(pairs);
        //    foreach (var word in pairs)
        //    {
        //        List<int> vs = dictionary[word];

        //        foreach (var index in vs)
        //        {
        //            if (vs[index - 1])
        //        }
        //    }
        //}
    }
}